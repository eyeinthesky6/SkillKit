# Roadmap

Planned work for SkillKit.

## Milestones
- v0.x: CLI baseline (tsk), sandbox validators, audits JSONL, built-in skills (fix/build/config), registry MVP, docs site
- v1.0: RPC server + VS Code stub, trust metadata & verification, CI release automation, skills gallery
- v1.x: Plugin system, performance and DX improvements
