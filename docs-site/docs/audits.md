# Audits

Skill runs produce JSONL audit logs for traceability.

- Location (default): `docs/audits/tool-reports/<skill>/<timestamp>.log`
- Each line is a JSON record with timestamp, event, metadata
- Use CLI to tail or summarize audits (planned)
